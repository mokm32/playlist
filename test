 const playlist = [
                {
                    title: "Astro Ria (A)",
                    url: "https://tinyurl.com/playria0",
                    logo: "https://aqfadtv.xyz/logos/Astro Ria (2024).png",
                    channelNumber: "104",
                    groupLogo: "https://i.ibb.co/mrNKFM6V/Untitled-design.png"
                }
            ];

            // Function to render the playlist
            function renderPlaylist() {
                playlistItems.innerHTML = '';
                playlist.forEach((video, index) => {
                    const li = document.createElement('li');
                    li.innerHTML = `
                        <img src="${video.logo}" class="channel-logo" alt="${video.title} logo">
                        <span>${video.title} (Channel ${video.channelNumber})</span>
                        <button onclick="playVideo(${index})">Play</button>
                    `;
                    playlistItems.appendChild(li);
                });
            }

            // Function to add video to the playlist
            addButton.addEventListener("click", function() {
                const title = videoTitleInput.value;
                const url = videoUrlInput.value;

                if (title && url) {
                    playlist.push({ title: title, url: url, logo: '', channelNumber: '', groupLogo: '' });
                    renderPlaylist();

                    // Clear input fields
                    videoTitleInput.value = '';
                    videoUrlInput.value = '';
                }
            });

            // Function to play selected video from the playlist
            window.playVideo = function(index) {
                const video = playlist[index];
                videoSource.src = video.url;
                videoPlayer.load();
                videoPlayer.play();
            };

            renderPlaylist(); // Initialize the playlist rendering
        });
    </script>
</body>
</html>
